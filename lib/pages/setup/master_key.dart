import 'package:flutter/material.dart';

class MasterKeyPage extends StatelessWidget {
  const MasterKeyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("master key page"));
  }
}
